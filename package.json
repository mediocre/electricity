{
  "description": "",
  "dependencies": {
    "mime": "1.2.x",
    "watch": "0.9.x",
    "node-sass": "0.8.x"
  },
  "devDependencies": {
    "mocha": "*",
    "express": "3.x",
    "buffer-compare": "0.0.1"
  },
  "keywords": [
    "asset",
    "static"
  ],
  "main": "./lib",
  "name": "electricity",
  "repository": "git://github.com/mediocre/electricity.git",
  "scripts": {
    "app": "node test/app.js",
    "test": "mocha --reporter spec -t 11000",
    "watch": "mocha --reporter spec -w -t 11000",
    "quicktest": "mocha --reporter spec -g watcher -i",
    "quickwatch": "mocha --reporter spec -w -g watcher -i"
  },
  "version": "0.0.7"
}
